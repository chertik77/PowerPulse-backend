model Product {
  id                   String @id @default(cuid())
  weight               Int
  calories             Int
  title                String
  category             String
  groupBloodNotAllowed Json   @map("group_blood_not_allowed")

  @@map("products")
}
